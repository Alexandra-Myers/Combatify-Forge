modLoader="javafml" #mandatory

loaderVersion="[43,)" #mandatory This is typically bumped every Minecraft version by Forge. See our download page for lists of versions.

license="GPL-3.ß"

[[mods]] #mandatory
# The modid of the mod
modId="atlascombat" #mandatory

version="1.1.7-1.19.2-Forge" #mandatory

displayName="Atlas Combat" #mandatory

logoFile="icon.png" #optional

credits="Original Creator of the Combat Test Snapshots: Jens Bergensten"

authors="Maintainer and Author: Alexandra, Assistence: zOnlyKroks" #optional

description='''A mod library and standalone mod that adds features from the combat test snapshots'''

[[dependencies.atlascombat]] #optional
    # the modid of the dependency
    modId="forge" #mandatory
    # Does this dependency have to exist - if not, ordering below must be specified
    mandatory=true #mandatory
    # The version range of the dependency
    versionRange="[43,)" #mandatory
    # An ordering relationship for the dependency - BEFORE or AFTER required if the relationship is not mandatory
    ordering="NONE"
    # Side this dependency is applied on - BOTH, CLIENT or SERVER
    side="BOTH"
# Here's another dependency
[[dependencies.atlascombat]]
    modId="minecraft"
    mandatory=true
# This version range declares a minimum of the current minecraft version up to but not including the next major version
    versionRange="[1.19.2,1.19.3)"
    ordering="NONE"
    side="BOTH"
